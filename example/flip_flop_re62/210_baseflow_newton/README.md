# flip_flop/210_baseflow_newton

**Stage**: baseflow Newton
**Variants**: upo

This stage has multiple variants that share the same dispatch subroutine but
use different parameter choices. Each variant lives in its own subdirectory.

Cross-variant residual decay plot: `scripts/nstab_compare.py example/flip_flop/210_baseflow_newton/`
